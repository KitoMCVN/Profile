"use client";
import { useLanguage } from "@/hooks/useLanguage";
import { TypeAnimation } from "react-type-animation";

export default function Home() {
  const { t, a, l, c } = useLanguage();

  return (
    <div className="h-[199vh]">
      <div></div>
    </div>
  );
}
